# This will use range() with a step of -1 to decrement the number of # symbols.

# Inverted triangle
for i in range(10, 0, -1):  # Start from 10, end at 1, step by -1
    print("#" * i)

# For a centered triangle, we calculate spaces and # symbols using range() in a standard increasing loop.

# Centered triangle
rows = 5
for i in range(rows):  # Increment i from 0 to 4
    spaces = " " * (rows - i - 1)  # Adjust number of spaces
    hashes = "#" * (2 * i + 1)  # Number of "#" based on 2 * i + 1
    print(spaces + hashes)

# This code creates an upper triangle and a mirrored lower triangle using range() with both positive and negative steps.

# Diamond shape
rows = 6

# Upper part of the diamond
for i in range(rows):  # Increment from 0 to 5
    spaces = " " * (rows - i - 1)
    hashes = "#" * (2 * i + 1)
    print(spaces + hashes)

# Lower part of the diamond
for i in range(rows - 2, -1, -1):  # Decrement from 4 to 0
    spaces = " " * (rows - i - 1)
    hashes = "#" * (2 * i + 1)
print(spaces + hashes)
