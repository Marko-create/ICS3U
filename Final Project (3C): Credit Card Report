file = "data.dat"
try:
    fh = open(file, "r")
    
[first, last, card, ccNum, month, year] = customer.split(",")

GivenName = first
Surname = last
CCType = card
CCNumber = ccNum
ExpMo = month
ExpYr = year

line1 = fh.readline().strip()
print (line1)
